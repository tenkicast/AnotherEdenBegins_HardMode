# Build output. bin/ is ~98 MB here because MSBuild copies every referenced
# game assembly next to the plugin - none of which are redistributable.
bin/
obj/

# Visual Studio / Rider / VS Code
.vs/
.idea/
.vscode/
*.user
*.suo

# Local overrides
*.local
launchSettings.json

# OS noise
Thumbs.db
desktop.ini
.DS_Store

# Publishing collateral - kept locally, never published.
# Marketing copy and donation text are not part of the mod.
NEXUS_DESCRIPTION.txt
KOFI.txt
